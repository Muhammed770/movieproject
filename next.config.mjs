/** @type {import('next').NextConfig} */

const nextConfig = {
    // async redirects() {
    //     if (process.env.NODE_ENV === "development") {
    //         return [
    //             {
    //                 source: '/api/:slug',
    //                 destination: 'http:0.0.0.0:3001/api/:slug',
    //                 permanent: true
    //             }
    //         ]
    //     }
    // }
};

export default nextConfig;
